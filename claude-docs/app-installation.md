Set `HOMEBREW_CASK_OPTS="--no-quarantine"` in shell init before installing — suppresses the Gatekeeper "downloaded from
the Internet" dialog.

| App            | Install                             |
|----------------|-------------------------------------|
| 1Password      | `brew install --cask 1password`     |
| BetterDisplay  | `brew install --cask betterdisplay` |
| Chromium       | `brew install --cask chromium`      |
| Codex CLI      | `brew install --cask codex`         |
| GitHub Desktop | `brew install --cask github`        |
| Granola        | `brew install --cask granola`       |
| iTerm2         | `brew install --cask iterm2`        |
| NordVPN        | `brew install --cask nordvpn`       |
| Notion         | `brew install --cask notion`        |
| Setapp         | `brew install --cask setapp`        |
| Slack          | `brew install --cask slack`         |
| TablePlus      | `brew install --cask tableplus`     |
| Zed            | `brew install --cask zed`           |
| Zight          | `brew install --cask zight`         |