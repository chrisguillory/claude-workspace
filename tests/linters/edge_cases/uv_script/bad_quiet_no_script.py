#!/usr/bin/env -S uv run --quiet --no-project
# /// script
# requires-python = ">=3.13"
# dependencies = [
#   "pydantic",
# ]
# ///
"""Bad shebang — --quiet --no-project with PEP 723 metadata but missing --script."""
