#!/usr/bin/env -S uv run --no-project --script
# /// script
# requires-python = ">=3.13"
# dependencies = [
#   "cc_lib",
#   "pydantic>=2.0.0"
# ]
# ///
"""Bad — multi-line, sorted, but last dep missing trailing comma."""
