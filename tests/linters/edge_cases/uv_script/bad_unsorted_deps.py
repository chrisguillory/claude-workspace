#!/usr/bin/env -S uv run --no-project --script
# /// script
# requires-python = ">=3.13"
# dependencies = [
#   "pydantic>=2.0.0",
#   "cc_lib",
# ]
# ///
"""Bad — multi-line with trailing comma but wrong sort order."""
