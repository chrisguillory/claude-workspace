.DS_Store
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/worktrees/
.venv/
__pycache__/
*.pyc
scratch/
target/

# per-session artifact store (machine-local)
/claude-sessions/
*.so

# CI caches (mypy incremental; xdist per-worker test scratch)
.mypy_cache/
.test-scratch/

# IDE
.idea/
.cursor/
.cursorrules
.vscode/
.specstory/

# Auth/session files (sensitive)
*_auth.json
*.auth.json
# Machine-private shell config — the real file lives out-of-tree (~/.config/zsh.local);
# any-depth belt-and-suspenders against an in-tree habit-drop
local.zsh
zsh.local

# brew bundle dropping (debugging artifact, not reproducible-install state)
Brewfile.lock.json

/.obsidian/
