[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "crb-nfsd"
version = "0.1.0"
description = "Userspace NFSv3 server for claude-remote-bash filesystem mounts"
requires-python = ">=3.13,<3.14"

[tool.maturin]
bindings = "bin"
manifest-path = "Cargo.toml"