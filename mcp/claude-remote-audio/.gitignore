# Compiled artifacts — kept locally for fast iteration; rebuilt by
# scripts/bootstrap.sh on each host. Source of truth is each .swift file.
swift/claude-coreaudio-volume
swift/claude-tcc-probe
swift/claude-coreaudio-probe
